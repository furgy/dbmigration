[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dbmigration"
version = "0.1.1"
description = "Database migration utilities"
requires-python = ">=3.9"
dependencies = []
